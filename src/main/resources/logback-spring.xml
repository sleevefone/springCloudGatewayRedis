<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{traceId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>

        <!--<?xml version="1.0" encoding="UTF-8"?>-->
        <!--<configuration>-->

        <!--    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>-->

        <!--    &lt;!&ndash;-->
        <!--        BEST PRACTICE FIX:-->
        <!--        Removed the <springProperty> tag which can have initialization order issues.-->
        <!--        Instead, we directly use the Spring Boot property in the pattern with a default value.-->
        <!--        Format: [application-name,traceId,spanId]-->
        <!--    &ndash;&gt;-->
        <!--    <property name="CONSOLE_LOG_PATTERN"-->
        <!--              value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p ${PID:- } -&#45;&#45; [%15.15t] [%-30.30logger{29}] [${spring.application.name:-openapi},%X{traceId:-},%X{spanId:-}] : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>-->

        <!--    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">-->
        <!--        <encoder>-->
        <!--            <pattern>${CONSOLE_LOG_PATTERN}</pattern>-->
        <!--            <charset>utf8</charset>-->
        <!--        </encoder>-->
        <!--    </appender>-->

        <!--    <root level="INFO">-->
        <!--        <appender-ref ref="CONSOLE"/>-->
        <!--    </root>-->

        <!--    <logger name="com.ocft.gateway.openapi" level="DEBUG" additivity="false">-->
        <!--        <appender-ref ref="CONSOLE"/>-->
        <!--    </logger>-->

        <!--</configuration>-->